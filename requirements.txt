# WerkstattArchiv - Requirements
# Python 3.11+

# GUI Framework
customtkinter>=5.2.0

# PDF-Verarbeitung
PyMuPDF>=1.23.0
pdf2image>=1.16.3  # Für PDF->Bild Konvertierung (PaddleOCR)

# OCR - PaddleOCR (keine externe Installation nötig, kein Tesseract erforderlich)
paddlepaddle>=2.6.0  # PaddlePaddle CPU-Version
paddleocr>=2.7.0    # OCR Engine mit deutschem Sprachmodell
Pillow>=10.0.0

# File-Monitoring (optional für zukünftige Erweiterungen)
watchdog>=3.0.0
